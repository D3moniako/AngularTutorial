import { HigelightDirective } from './higelight.directive';

describe('HigelightDirective', () => {
  it('should create an instance', () => {
    const directive = new HigelightDirective();
    expect(directive).toBeTruthy();
  });
});
